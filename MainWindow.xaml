<Window x:Class="Converter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Converter"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        MinWidth="500"
        MinHeight="400">
        
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>

            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>


        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical">
            <Button x:Name="UploadFile" Grid.Column="1" Grid.Row="1" Click="UploadFile_Click" Height="200" Content="Upload" FontSize="30"/>
            <Button x:Name="SaveFile"  Grid.Column="1" Grid.Row="1" Click="SaveFile_Click" Height="240" Content="Save" FontSize="30"/>
        </StackPanel>

        <Menu Grid.Column="1">
            <MenuItem Header="файл">
  
                <Separator/>
                <MenuItem Header="Выбрать формат">

                    <MenuItem x:Name="JpgItem" Header="JPG"  IsCheckable="True" Click="FormatItem_Click"></MenuItem>
                    <MenuItem x:Name="PngItem" Header="PNG"  IsCheckable="True" Click="FormatItem_Click"></MenuItem>
                    <MenuItem x:Name="BmpItem" Header="BMP"  IsCheckable="True" Click="FormatItem_Click"></MenuItem>
                    <MenuItem x:Name="tiffItem" Header="TIFF"  IsCheckable="True" Click="FormatItem_Click"></MenuItem>
  
                </MenuItem>
            </MenuItem>
        </Menu>


        <Image x:Name="MyImage" Grid.Column="2" Margin="30"></Image>
    </Grid>
</Window>
